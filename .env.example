SECRET_KEY=your-secret-key-here
# Set to True for local dev, False/omitted in production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
# Render auto-sets RENDER=true and RENDER_EXTERNAL_HOSTNAME — no manual config needed

# Database: Set DATABASE_URL for Neon PostgreSQL (Render/production)
# DATABASE_URL=postgresql://user:password@host:5432/dbname
# Or use individual params for local dev (falls back to SQLite if neither is set)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=acps
DB_USER=acps_user
DB_PASSWORD=your-password
DB_HOST=localhost
DB_PORT=5432

CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
TIME_ZONE=Africa/Addis_Ababa
